# Do-able — localStorage Data API Reference

> There is no backend. All data is stored in the browser's `localStorage`.
> This document describes the data structures and the JavaScript functions that
> replace the original REST API.

## Conventions

- `localStorage` keys are prefixed with `doable_`
- Dates: ISO 8601 (`2026-06-25`) for due dates, ISO datetime for timestamps
- All IDs are unique strings generated by `Date.now().toString(36) + Math.random().toString(36).slice(2,6)`
- Soft-delete: tasks with `deleted_at` set are hidden from main views, visible in Bin

## Storage Keys

### `doable_tasks`

Array of task objects.

```json
[
  {
    "id": "k3x8a9b2c",
    "title": "Buy milk",
    "description": "",
    "status": "not_started",
    "priority": "high",
    "due_date": "2026-06-25",
    "category": "personal",
    "tags": ["errands"],
    "fields": {},
    "recur": null,
    "depends_on": [],
    "notes": [],
    "created_at": "2026-06-21T10:00:00.000Z",
    "updated_at": "2026-06-21T10:00:00.000Z",
    "deleted_at": null
  }
]
```

### `doable_activity`

Chronological array of activity entries (newest first). Keeps last 500.

```json
[
  {
    "id": "m4x9c2d1e",
    "task_id": "k3x8a9b2c",
    "action": "created",
    "details": "Buy milk",
    "timestamp": "2026-06-21T10:00:00.000Z"
  }
]
```

Actions: `created`, `completed`, `deleted`, `restored`, `updated`

### `doable_config`

```json
{
  "theme": "nord-dark",
  "date_mode": "smart",
  "per_page": 25
}
```

- `theme`: `"nord-dark"` | `"nord-light"`
- `date_mode`: `"smart"` (relative) | `"iso"`
- `per_page`: integer, pagination size

### `doable_notes`

Array of scratch pad notes.

```json
[
  {
    "id": "n1a2b3c4d",
    "text": "Ideas for project...",
    "pinned": true,
    "created_at": "2026-06-21T10:00:00.000Z",
    "updated_at": "2026-06-21T10:00:00.000Z"
  }
]
```

### `doable_focus`

Map of date → task ID array. Auto-clears old dates.

```json
{
  "2026-06-21": ["k3x8a9b2c", "m7d4e5f6g"]
}
```

## Functions (JS API)

All functions are defined in `doable.html` and globally accessible.

| Function | Description |
|----------|-------------|
| `getTasks()` | Returns array of all tasks |
| `saveTasks(tasks)` | Replaces stored tasks array |
| `getActivity()` | Returns activity log |
| `saveActivity(entries)` | Replaces stored activity |
| `getConfig()` | Returns config object |
| `saveConfig(config)` | Saves config |
| `getNotes()` | Returns scratch notes |
| `saveNotes(notes)` | Saves scratch notes |
| `getFocus()` | Returns focus goals |
| `saveFocus(focus)` | Saves focus goals |
| `logActivity(taskId, action, details)` | Adds entry to activity log, auto-truncates to 500 |
| `showTaskDetail(id)` | Opens task detail modal |

## Data Flow

```
User Action → Event Handler → JS Function → localStorage (sync)
                                    ↓
                              Re-render View
```

- No async operations. All reads and writes are synchronous `localStorage` operations.
- Mutations are followed immediately by re-rendering the current view.
- Activity log is written on every mutation (create, update, delete, restore, complete).

## Features

| Feature | Implementation |
|---------|---------------|
| **Recurring** | On marking done, `handleRecurrence()` creates next instance |
| **Soft-delete** | `deleted_at` timestamp set, hidden from main views |
| **Annotations** | Notes array on task object, shown in detail modal |
| **Dependencies** | `depends_on` array of task IDs, informational only |
| **Search** | Client-side `String.includes()` across title + description |
| **Relative dates** | `fmtDate()` converts ISO to "today", "in 3 days", etc. |
| **Export** | `exportData()` generates JSON/CSV/MD and triggers download |
| **Backup/restore** | JSON file download/upload of all localStorage data |
| **Focus goals** | Per-date array in `doable_focus`, auto-clears daily |
| **Overdue detection** | Client-side comparison of `due_date` against today |
| **Activity log** | Array in localStorage, keeps 500 entries |
| **Theme** | CSS custom properties, `data-theme` attribute toggle |
